import { useEffect, useMemo, useState } from "react";
import { Helmet } from "react-helmet-async";
import { ArrowRight, Building2, CalendarDays, CheckCircle2, ClipboardList, Clock3, ExternalLink, Handshake, LogOut, RefreshCw, Users } from "lucide-react";

type FollowUp = { followup_ref: string; entity_type: string; entity_ref: string; notes?: string; due_at?: string; status: string };
type Activity = { activity_ref: string; summary?: string; activity_type: string; entity_ref: string; created_at?: string };
const date = (value?: string) => value ? new Date(value).toLocaleString("en-IN", { dateStyle: "medium", timeStyle: "short" }) : "Not scheduled";

export default function OperationsAdmin() {
  const [data, setData] = useState<any>({ enquiries: [], visits: [], deals: [], buyers: [] });
  const [activity, setActivity] = useState<{ activities: Activity[]; followups: FollowUp[] }>({ activities: [], followups: [] });
  const [csrf, setCsrf] = useState(""); const [email, setEmail] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  async function load() { setLoading(true); setError(""); try { const responses = await Promise.all([fetch("/api/enquiry-admin", { cache: "no-store" }), fetch("/api/deal-operations", { cache: "no-store" }), fetch("/api/activity-operations", { cache: "no-store" })]); if (responses.some((r) => r.status === 401)) { setEmail(""); return; } const [enquiries, deals, activities] = await Promise.all(responses.map((r) => r.json())); if (!responses.every((r) => r.ok)) throw new Error(enquiries.error || deals.error || activities.error || "Could not load operations."); setData({ enquiries: enquiries.enquiries || [], visits: enquiries.visits || [], deals: deals.deals || [], buyers: deals.buyers || [] }); setActivity({ activities: activities.activities || [], followups: activities.followups || [] }); setCsrf(activities.csrf || enquiries.csrf || deals.csrf || ""); setEmail(activities.email || enquiries.email || deals.email || ""); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load operations."); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  const followups = useMemo(() => activity.followups.filter((item) => item.status === "OPEN"), [activity.followups]);
  const newEnquiries = useMemo(() => data.enquiries.filter((item: any) => item.status === "NEW").length, [data.enquiries]);
  async function complete(item: FollowUp) { try { const response = await fetch("/api/activity-operations", { method: "POST", headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf }, body: JSON.stringify({ action: "FOLLOWUP_STATUS", entityType: item.entity_type, entityRef: item.entity_ref, followupRef: item.followup_ref, status: "DONE" }) }); if (!response.ok) throw new Error("Could not complete follow-up."); await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not complete follow-up."); } }
  async function logout() { await fetch("/api/property-auth?action=logout", { method: "POST" }); window.location.assign("/admin/login"); }
  if (!email && !loading) return <main className="grid min-h-screen place-items-center bg-slate-950 p-6 text-white"><Helmet><title>ARE Admin</title><meta name="robots" content="noindex,nofollow"/></Helmet><section className="w-full max-w-md border border-white/15 bg-white/5 p-8"><p className="text-xs font-semibold uppercase tracking-[.2em] text-white/55">Anantha Real Estate</p><h1 className="mt-3 font-display text-4xl font-semibold">Operations access</h1><p className="mt-4 leading-relaxed text-white/70">Sign in to manage enquiries, listings, site visits and deals.</p><a href="/admin/login" className="mt-7 inline-flex bg-white px-5 py-3 text-sm font-semibold text-slate-950">Admin sign in <ArrowRight className="ml-2" size={16}/></a></section></main>;
  const cards = [["New enquiries", newEnquiries, ClipboardList, "/admin/enquiries", "Needs first response"], ["Open follow-ups", followups.length, Clock3, "#followups", "Due actions"], ["Site visits", data.visits.length, CalendarDays, "/admin/enquiries", "Scheduled and complete"], ["Active deals", data.deals.length, Handshake, "/admin/deals", "Buyer conversations"]] as const;
  return <main className="min-h-screen bg-[#f6f6f3] text-slate-950"><Helmet><title>Operations Dashboard | Anantha Real Estate</title><meta name="robots" content="noindex,nofollow"/><meta name="referrer" content="no-referrer"/></Helmet><div className="mx-auto max-w-7xl px-4 py-5 md:px-8 md:py-8"><header className="flex flex-col gap-5 border-b border-slate-200 pb-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-slate-500">Anantha Real Estate · Internal</p><h1 className="mt-3 font-display text-4xl font-semibold md:text-5xl">Operations overview</h1><p className="mt-3 text-sm text-slate-600">A single view of incoming demand and the work that moves it forward.</p></div><div className="flex flex-wrap gap-2"><a href="/" target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold">Website <ExternalLink size={15}/></a><button onClick={() => void load()} className="inline-flex items-center gap-2 border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold"><RefreshCw size={15} className={loading ? "animate-spin" : ""}/>Refresh</button><button onClick={() => void logout()} className="inline-flex items-center gap-2 bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white"><LogOut size={15}/>Sign out</button></div></header><nav className="grid gap-px border-x border-b border-slate-200 bg-slate-200 sm:grid-cols-4">{[["Properties", "/admin/properties", Building2], ["Enquiries", "/admin/enquiries", ClipboardList], ["Deals", "/admin/deals", Handshake], ["GBP", "/admin/gbp", Users]].map(([label, href, Icon]: any) => <a key={label} href={href} className="flex items-center justify-between bg-white px-5 py-4 text-sm font-semibold hover:bg-slate-50">{label}<Icon size={17}/></a>)}</nav>{error && <p className="mt-6 border border-rose-200 bg-rose-50 p-4 text-sm text-rose-800">{error}</p>}<section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label, value, Icon, href, note]) => <a href={href} key={label} className="border border-slate-200 bg-white p-5 hover:border-slate-950"><div className="flex items-start justify-between"><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-500">{label}</p><Icon size={19}/></div><p className="mt-6 font-display text-5xl font-semibold">{value}</p><p className="mt-3 text-sm text-slate-500">{note}</p></a>)}</section><section className="mt-8 grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><div id="followups" className="border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-200 p-5"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-500">Priority queue</p><h2 className="mt-1 font-display text-2xl font-semibold">Follow-ups to complete</h2></div><span className="grid h-9 min-w-9 place-items-center rounded-full bg-slate-950 px-2 text-sm font-semibold text-white">{followups.length}</span></div>{followups.length ? followups.map((item) => <article key={item.followup_ref} className="border-b border-slate-100 p-5 last:border-0"><div className="flex flex-col gap-4 sm:flex-row sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-slate-500">{item.entity_type} · {item.entity_ref}</p><p className="mt-2 font-medium">{item.notes || "Follow up with this record."}</p><p className="mt-2 text-sm text-slate-500">Due {date(item.due_at)}</p></div><button onClick={() => void complete(item)} className="inline-flex h-fit w-fit items-center gap-2 border border-slate-950 px-3 py-2 text-sm font-semibold hover:bg-slate-950 hover:text-white"><CheckCircle2 size={15}/>Done</button></div></article>) : <div className="p-8"><CheckCircle2 className="text-emerald-600" size={25}/><h3 className="mt-4 font-display text-2xl font-semibold">Nothing needs attention now.</h3><p className="mt-2 text-sm text-slate-600">New follow-ups will appear here as they are created.</p></div>}</div><div className="border border-slate-200 bg-white"><div className="border-b border-slate-200 p-5"><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-500">Activity feed</p><h2 className="mt-1 font-display text-2xl font-semibold">Recent changes</h2></div><div className="max-h-[520px] overflow-y-auto">{activity.activities.slice(0,30).map((item) => <article key={item.activity_ref} className="border-b border-slate-100 p-5 last:border-0"><p className="text-sm font-semibold">{item.summary || item.activity_type}</p><p className="mt-1 text-xs text-slate-500">{item.entity_ref} · {date(item.created_at)}</p></article>)}{!activity.activities.length && <p className="p-8 text-sm text-slate-500">Activity will appear here as records are updated.</p>}</div></div></section></div></main>;
}

